app:
   name: Micro Service Level 3
   description: Welcome to ${app.name}

my:
   greeting: Hello World
   list.values: one,two,three

db:
   connection: "{connectionString: 'Dev Profile Active', username: 'foo', password: 'foo'}"
   host: 129.0.0.1
   port: 2400

management.endpoints.web.exposure.include: "*"